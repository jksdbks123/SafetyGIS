# SafetyGIS Development Log

The old long-form development log has been retired to keep the repository focused.

Current source of truth:

- `PROJECT_STATE.md` for accepted project state.
- `METHODOLOGY.md` for the EPDO safety-ranking method.
- `Plan.md` only records that the previous roadmap is obsolete.

Future entries should be short and decision-oriented.
