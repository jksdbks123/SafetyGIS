# SafetyGIS Agent Notes

This file is intentionally minimal. Use `AGENTS.md` and `PROJECT_STATE.md` for
current repository guidance.

Core rules:

- User-facing assistant responses should be Chinese.
- Product output should be English.
- Keep the app runnable while prioritizing the intersection and roadway model.
- Treat previous phase plans as obsolete.
- Keep generated caches and large local datasets out of git.
