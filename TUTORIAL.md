# SafetyGIS Tutorial Status

The previous deployment tutorial is retired for now.

Use `README.md` for local startup and Docker basics. Deployment details should be
rewritten only after the intersection and roadway model is stable enough to justify
another public deployment pass.
